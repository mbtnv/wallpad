services:
  wallpad:
    build: .
    container_name: wallpad
    env_file:
      - .env
    volumes:
      - ./dashboard.yaml:/app/dashboard.yaml:ro
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}"
    restart: unless-stopped
